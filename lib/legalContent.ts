// Shared between the public Terms/Privacy/Cancellation Policy pages
// (fallback when the site_settings row is unset) and the admin Settings
// form (pre-filled default value), same reasoning as HERO_TEXT_DEFAULTS in
// lib/hero.ts.

export const DEFAULT_TERMS_CONTENT = "Terms and conditions content goes here.";

export const DEFAULT_PRIVACY_POLICY_CONTENT = "Privacy policy content goes here.";

export const DEFAULT_CANCELLATION_POLICY_CONTENT = `Cancellation and refund terms vary by tour and journey; they depend on factors like supplier deposits, park permits, and accommodation booking terms, so we set them per package rather than a single blanket policy.

You'll find the specific cancellation terms for a package on its detail page, and your travel consultant will confirm them again before you book. If you're not sure about a package you're considering, just ask before you enquire.`;
